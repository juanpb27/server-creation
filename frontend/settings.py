url_env = "http://127.0.0.1"
port = ":5000"

link_names = {
    "token" : url_env + port + "/token",
    "images" : url_env + port + "/images",
    "networks": url_env + port + "/networks",
    "flavors": url_env + port + "/flavors",
    "keypairs": url_env + port + "/keypairs",
    "security_groups" : url_env + port + "/security_groups"
}